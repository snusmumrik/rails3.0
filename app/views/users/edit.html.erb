<h2>Edit Your Account</h2>

<% if @user.errors.any? %>
  <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>

<%= render "form" %>

<p><%= link_to t('link.user.show'), user_path(@user) %> | <%= link_to t('link.password.change_password'), change_password_path %></p>
